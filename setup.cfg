# Helper file to handle all configs

[coverage:run]
# .coveragerc to control coverage.py and pytest-cov
omit =
    # Omit the tests
    */tests/*
    # Omit generated versioneer
    lindelint/_version.py

[versioneer]
# Automatic version numbering scheme
VCS = git
style = pep440
versionfile_source = lindelint/_version.py
versionfile_build = lindelint/_version.py
tag_prefix = ''

[aliases]
test = pytest
